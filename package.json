{
  "name": "znb-force-plus",
  "version": "1.0.0",
  "description": "ZnB Force Plus - Insurance Management Application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev:pocketbase": "cd apps/pocketbase && npm run dev",
    "install:all": "npm install && cd apps/api && npm install && cd ../web && npm install && cd ../pocketbase && npm install && cd ../..",
    "build": "npm run build --workspaces",
    "start": "npm start --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
